<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create New Product</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <% if (error.length>0) { %>
        <div id="alertBox" class="absolute left-1/3 bg-red-500 text-white px-4 py-3 rounded-md flex items-center justify-between w-full max-w-md mx-auto shadow-lg">
            <span>⚠️ <%= error %></span>
            <button onclick="dismissAlert()" class="text-white font-bold text-lg">&times;</button>
        </div>
    <% } %>
    <% if (success.length>0) { %>
        <div id="alertBox" class="absolute left-1/3 bg-green-500 text-white px-4 py-3 rounded-md flex items-center justify-between w-full max-w-md mx-auto shadow-lg">
            <span>✅ <%= success %></span>
            <button onclick="dismissAlert()" class="text-white font-bold text-lg">&times;</button>
        </div>
    <% } %>
    <div class="w-full h-screen p-0 m-0">
        <nav class="flex items-center justify-between pt-6 px-12">
            <div>
                <h3 class="text-3xl p-4">Admin Panel.</h3>
            </div>
            <div class="w-3/4 flex gap-4 items-center justify-end">
                <a class="p-4" href="/">Home</a>
                <a class="p-4" href="#">Products</a>
                <a class="p-4" href="/user/profile-details">My account</a>
                <button onclick="logoutClick()" class="p-4 font-medium text-red-500">Log out</a>
            </div>
        </nav>
        <hr>
        <main class="flex">
            <div class="sidebar w-1/6 pl-8 pt-12">
                <p class="text-gray-600 cursor-pointer">All Products</p>
                <p class="cursor-pointer">Create New Product</p>
            </div>
            <div class="create w-full">
                <form action="/product/create" enctype="multipart/form-data" method="post" class="p-12">
                    <p class="text-2xl font-bold mb-3">Create New Product</p>
                    <hr>
                    <p class="text-xl font-medium mb-3 mt-3">Product Details</p>
                    <div class="flex gap-5 mb-3">
                        <p>Product Image</p>
                        <input type="file" name="image" required>
                    </div>
                    <input type="text" placeholder="Product Name" name="name" class="w-1/3 px-4 py-2 rounded border border-gray-400 mb-3" required>
                    <input type="text" placeholder="Product Price" name="price" class="w-1/3 px-4 py-2 ml-3 rounded border border-gray-400 mb-3" required><br>
                    <input type="text" placeholder="Discount (In Percentage)" name="discount" class="w-1/3 px-4 py-2 rounded border border-gray-400 mb-3">
                    <p class="text-xl font-medium mt-2 mb-3">Product Details</p>
                    <input type="text" placeholder="Background Color" name="bgColor" class="w-1/3 px-4 py-2 rounded border border-gray-400 mb-3">
                    <input type="text" placeholder="Panel Color" name="panelColor" class="w-1/3 px-4 py-2 ml-3 rounded border border-gray-400 mb-3"><br>
                    <input type="text" placeholder="Text Color" name="textColor" class="w-1/3 px-4 py-2 rounded border border-gray-400 mb-3"><br>
                    <input type="submit" class="px-4 py-2 rounded-md bg-blue-500 text-white" value="Create New Product">
                </form>
            </div>
        </main>
    </div>
    <script>
        function dismissAlert() {
          document.getElementById("alertBox").style.display = "none";
        }
        function logoutClick() {
            if(confirm("Are you sure you want to log out?")){
                location.href = "/user/logout"
            }
        }
    </script>
</body>

</html>